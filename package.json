{
  "name": "hoorstyle-hostinger-node-wrapper",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "index.js",
  "scripts": {
    "build": "cd app && npm install && npm run build",
    "start": "node index.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "engines": {
    "node": "22.x",
    "npm": ">=10"
  },
  "description": "Hostinger-compatible Express wrapper that builds and starts the HoorStyle TanStack app from /app"
}
